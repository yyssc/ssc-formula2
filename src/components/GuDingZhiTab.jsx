import React from 'react';
import { forbidExtraProps } from 'airbnb-prop-types';

const propTypes = forbidExtraProps({
});

export const defaultProps = {
};


export default class GuDingZhiTab extends React.Component {
  // constructor(props) {
  //   super(props);
  // }

  componentDidMount() {
  }

  /**
   *
   *
   * @param {any} nextProps
   *
   * @memberof GuDingZhiTab
   */
  componentWillReceiveProps() {
  }

  /**
   *
   *
   * @param {any} nextProps
   * @param {any} nextState
   *
   * @memberof GuDingZhiTab
   */
  // shouldComponentUpdate() {
  // }

  /**
   *
   *
   * @param {any} prevProps
   * @param {any} prevState
   *
   * @memberof GuDingZhiTab
   */
  componentDidUpdate() {
  }

  render() {
    return (
      <div>
        101
      </div>
    );
  }
}

GuDingZhiTab.propTypes = propTypes;
GuDingZhiTab.defaultProps = defaultProps;
